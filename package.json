{
  "name": "agent-travel",
  "version": "1.0.0",
  "description": "智能旅游助手 — AI 驱动的旅游规划平台",
  "private": true,
  "scripts": {
    "dev:server": "cd travle-server && npm run dev",
    "dev:client": "cd agentTravel && npm run dev",
    "dev": "concurrently -n server,client -c blue,green \"npm run dev:server\" \"npm run dev:client\"",
    "install:all": "cd travle-server && npm install && cd ../agentTravel && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
